RewriteEngine On

# ถ้าเป็นไฟล์/โฟลเดอร์จริง ปล่อยผ่าน
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# ที่เหลือให้โยนเข้าโฟลเดอร์ web (rewrite ภายใน)
RewriteRule ^(.*)$ web/$1 [L]

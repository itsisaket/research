Options +FollowSymLinks
IndexIgnore */*

RewriteEngine on
RewriteBase /research/web/

# ถ้าเป็นไฟล์จริงหรือโฟลเดอร์จริง ให้เสิร์ฟตรง ๆ
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# อื่น ๆ ส่งเข้า index.php
RewriteRule . index.php [L]

RewriteEngine on

# ถ้าเป็นไฟล์จริง/โฟลเดอร์จริง ให้เสิร์ฟโดยตรง
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# อื่น ๆ ส่งเข้า index.php
RewriteRule . index.php [L]
RewriteEngine On
RewriteBase /research/web/

# ไฟล์/โฟลเดอร์จริงใน web ให้ผ่าน (assets, css, js, uploads)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# ที่เหลือส่งเข้า index.php
RewriteRule ^ index.php [L]
